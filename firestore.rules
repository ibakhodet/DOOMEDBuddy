rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    match /warbands/{playerId} {
      // Any authenticated user on the allow-list can read/write.
      // The code-level email allow-list prevents unauthorized access.
      allow read, write: if request.auth != null;
    }
  }
}
